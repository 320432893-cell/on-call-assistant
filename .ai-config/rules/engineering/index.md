# 专题索引: 代码语义

本专题只保留代码结构坏味道、非静态语义审查和抽象取舍规则。

除 `.ai-config/AGENTS.md` 的代码审查门禁要求外，不自动读取本专题。

## 触发入口

### 代码抽象与鲁棒性

命中以下任一场景，读取 `code.index.md`:

- 单次写入或重写代码超过 50 行。
- 写完一轮代码后，静态工具或 diff 暴露结构坏味道，需要判断局部修还是范式治理。
- 中大改动在动手前已经明确涉及状态生命周期、多 handler、错误分流、外部依赖隔离或公共行为稳定性。
- 涉及状态机、策略、注册表、错误层级、端口 / 适配器等范式取舍。
- 涉及失败路径、异常日志、调用方依赖、鲁棒性验证。

## 读取边界

- 机械格式、lint、类型、基础测试优先交给工具，不在 Markdown 中展开。
- 架构、API、Web 前端、GUI 等项目交付体检规则已迁到 `delivery/`。
- 老项目小改优先读相关代码和影响面；只有写后命中结构坏味道或高复杂度例外时，才读 `code.index.md`，不借机升级架构或重写范式。
