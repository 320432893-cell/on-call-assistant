# 审查路由(tier1;细则 daily.md/stage.md;触发指针 AGENTS §3)

人贴路径即执行;机器供 scope,判断/批准留人。

不变量(违则停):
- 时机=人(进度,机器拿不到);承重=机器扇入/`check_cohesion`(非人眼);放行批准=人(AI 只提议+证据,闸红逼审);行为安全=测试。
- 成本∝1/频率:**per-commit 禁全仓扫描**;体检/深审才放贵活(人触发)。
- **绿 sweep ≠ 架构无恙,只证 L4/L5。**
- **拆分判据=连通度非行数**:拆前必跑 `check_cohesion`,最大连通块≥80%=内聚不拆(硬拆=造循环依赖),多弱连通子簇才拆。

五层(审从上往下,上层没过别下层):
| 层 | 裁判 |
|---|---|
| L1 依赖方向/分层 | import-linter + 人定契约对不对 |
| L2 归属/双源 | def·class 名近似+签名+扇入;不同名→子agent对抗 |
| L3 契约/不变量 | 真测试(跑出来) |
| L4 死码/形状 | vulture/grimp扇入/radon/check_cohesion |
| L5 整洁 | ruff/check_naming |

路由:收工→`daily.md`(地板);切片闭包·交接前·合main前·动大东西前→`stage.md`(顶层)。
两份产出皆:待办清单 + 放行 pending(待人批)+ 漂移快照,全落文件。
