# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/

# 虚拟环境
.venv/
venv/
env/

# 环境变量 / 密钥
.env
.env.local
*.key

# 索引数据（运行时生成，不入库）
indexes/
data/processed/
data/raw/

# 运行截图（体积大、非源码）
screenshot/
prompt/screenshot_*.png

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp

# AI 工具本地配置（机器/账号专属，不入仓）
.claude/

# AI 共享配置中含 token 的实文件不入仓，只入 template
.ai-config/settings.json

# 测试 / 缓存
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage

# 模型权重文件（仅忽略实际权重文件，不忽略 app/models/ 等代码目录）
*.bin
*.safetensors
*.pt
*.pth
*.onnx
*.gguf
# 仅顶层根目录的 models/ 目录（HF 模型缓存等），不影响 app/models/
/models/

# 个人简历（按题面要求放在仓库根目录，但不入 git 跟踪）
/*.pdf
