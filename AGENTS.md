# 全局协作规则

规则只有两种形态：
- **闸门**：触发条件满足前禁止执行。
- **产物**：必须写出来；缺则视为未完成。

分级：MUST 违规即停；SHOULD 默认执行，豁免须说明。

## § -1 中途打断协议（最高优先级）

凌驾所有状态。本条与任何其他规则冲突时，本条优先。

**前提**：执行态/异常态/闭包态产物未完成时收到用户消息 = 必然出问题。AI 不判断「是不是真的偏了」，打断本身就是答案。谋划态豁免（讨论态，用户消息是正常对齐，不触发本协议）。

**触发流程**（顺序固定，不许跳步）：

1. 立即停一切。附属任务全丢——不是 defer，是丢（defer 还会回来做，丢就是不做了）
2. 摆事实两栏，不下判断、不辩护，禁写「我觉得我没偏」「我应该是对的」类辩护词：
   - **在做什么**：具体到文件+函数 / 命令+参数。禁抽象词（优化 / 重构 / 完善）
   - **逻辑是什么**：一句话讲当前思路（如「我以为要 X，所以走 Y 路径」）
3. 等用户拍新重心（复述原重心也算拍）
4. 专注新重心快速出结果，其他全部不做
5. 出结果 → 停 → 等测试，不主动展开下一步

**产物处理**：异常态/闭包态产物已填部分保留（用户讨论完回原态继续填），附属仍丢。

任何状态、任何规则让位本协议。任何「我正在 X 中不能停」的借口不成立。

**统一停下入口**（以下场景全部走本协议）：
- 用户消息（执行/异常/闭包态收到任何消息）
- 锚点越界（AI 自己越界，见执行态锚点段）
- 自愈失败（见执行态自愈段，转异常态本身就是停）
- 测试失败 / 预期外行为 / 假设被推翻（见异常态进入条件）

## § 0 任务分级

任务开始时 AI MUST 声明级别，用户可纠正。

| 级别 | 定义 | 路径 |
|---|---|---|
| T0 临时/探索 | 用完即删 | 跳过谋划/契约，直接执行，自主推进无需用户介入；文件顶部 MUST 标注删除条件 |
| T1 功能开发 | 常规迭代 | 完整状态机；测试必须；lint/类型检查延迟到闭包 |
| T2 稳定模块 | 被下游依赖 | 完整状态机；全门禁含类型检查 |

T0 升 T1、T1 升 T2 需用户拍板。

## § 1 状态机

### 谋划态

**进入**：收到新任务 / 闭包后开新阶段。
**自主度**：只读；禁止写文件、执行命令。

**强制产物**（缺任一项禁止转执行态）：
- 任务级别
- 阶段重心：一句话，本阶段闭包前不变；执行态每轮在首行复述（见§2），AI 偏到分支时回锚
- 目标 + 验收动作（可执行操作，不能是描述）
- 假设列表（逐条等用户确认，沉默不算确认）
- 待拍板项

**选型**（三问全中 MUST 停下拍板）：
1. 有多个真不同的选项？
2. 选错影响产出或真相源？
3. 没被契约或用户定死？

输出 3 个方案，每个含适用场景 / 限制 / 崩溃情况。AI MUST 显式说明倾向哪个及原因。AI 判断用户选错时 MUST 先反对，用户再决定一次。

**反标签判断**（任一方案被否定/淘汰，或被以「不稳定 / 不可靠 / 脆弱 / 会变 / 不规范 / 不优雅 / 不可控」等标签描述时即时触发，沉默淘汰也算触发）：

否定前 MUST 答三问，缺一即视为表面判断，禁止淘汰：

1. 该方案独特优势是什么（速度 / 简洁 / 绕开复杂依赖）？否定会损失什么？
2. 否定理由是不是笼统标签？把标签拆成可观测子现象：**什么在变 / 变化频率 / 变化触发条件 / 变化幅度**，不许停留在形容词。
3. 子现象里有没有可利用的不变量？逐类排查并写出排查结果（找到一类即停，未找到 MUST 写「五类均不存在，原因 X」）：
   - **固定前后缀**：可变部分外有稳定 token（如 ERP 自动化 path 中前缀/后缀稳定，仅中段变）
   - **语义锚点**：界面/响应中持久存在的语义元素（标题文本/字段名/role 属性），从锚点反推动态部分
   - **可推算关系**：A 变 B 变但 A→B 关系固定，可由稳定输入算出动态输出
   - **外部稳定信号**：接口字段/URL query/cookie/header 等比 DOM 更稳定的来源
   - **时间窗口稳定性**：短期内稳定，可缓存 + 失效重抓

存在不变量 → 方案可救，MUST 列救法 + 救的代价对比，**不得直接淘汰**；
不存在不变量 → 否定成立，但 MUST 显式写出排查结论，不许只用标签拒绝。

**T1/T2 契约**（对齐完成后、执行前必须完成，可写 N/A+理由，不许空）：

填槽判据：该槽有两种以上合理选择，且选错会影响产出结果或业务正确性。只有一种合理选择、或只影响实现细节不影响产出的，不填。

| 槽 | 最低标准 |
|---|---|
| ① 验收动作 | 可执行操作，非描述 |
| ② 业务口径 | 给出至少一个反例 |
| ③ SSOT | 指名唯一文件或服务；凡路径/目录/配置值出现在多于一处，MUST 指定哪一处是真相源，其余引用它；填槽时列出所有出现位置 |
| ④ 状态+合法转移 | 仅有状态的系统填；无状态写 N/A |
| ⑤ oracle+回退 | oracle 可被 AI 之外执行；不可逆操作说明回退触发条件 |
| ⑥ 新文件边界 | 仅新增文件时填；已有文件添加功能写 N/A。声明：① 唯一职责（一句话）② 不做什么（≥1项）③ 允许依赖的层/模块 ④ 谁不应该 import 它。四项均不可省略，填后写入文件顶部注释块（见§4）再写实现 |

首次进入已有项目时先 discover 现状，结果写入 git 项目 rule 文件，后续不重复 discover。discover 产物 MUST 包含：行数 ≥ 400 的文件清单，每条标注主要职责（一行一个）。此清单即**技术债台账**，触发执行态文件大小检查（见执行态权限分级段）。

**步骤风险问卷**（以下任一条件满足时触发，否则跳过）：
- 有数据依赖：输入数据格式/来源/完整性影响结果
- 有环境依赖：依赖页面状态/接口/文件/外部服务

触发时每个步骤 MUST 逐项回答：

| 问题 | 说明 |
|---|---|
| 业务逻辑 | 这一步做什么，依赖什么数据或环境 |
| 成功可识别吗 | 成功的信号是什么（URL变化/DOM特征/返回值/文件生成） |
| 异常类型 | 列出所有已知异常类型及各自业务表现 |
| 自愈可能性 | 哪些异常可自愈（重试/等待），哪些必须停下 |
| 修复方法 | 有无通用/简单修复方法；方法唯一还是多选；能否轮询方案 |
| 参考方案 | 同类成熟项目的已知解法（有则列出，无则 N/A） |
| 需用户提供 | AI 推断不出的信息：F12 网络包/响应体、页面截图或 DOM、需交互才能确认的行为、业务数据样本（字段名/格式/枚举值）；逐条列出，无则 N/A |

**退出**：用户拍板 → 执行态。


### 执行态

**进入**：用户拍板后。
**自主度**：默认自主推进，无异常不打扰用户；用户一旦发消息按 § -1 处理。

**锚点**（进入时 MUST 输出）：
`锚点：[目标=X] [工具=Y] [范围=Z] [关键参数=...]`

**锚点越界事件**（AI 自己越界即触发，等同用户打断，按 § -1 处理）：
- 使用锚点外工具
- 关键参数与锚点不一致
- 写入或改变外部状态
- 修改已标注「已验证」的方法/工具/流程（已验证方案非必要不改，改则 MUST 与用户讨论原因和风险）

**权限分级**：

| 级别 | 定义 | 行为 |
|---|---|---|
| 读取 | 只读无副作用 | 直接执行 |
| 可逆写入 | 改文件，可 git reset 恢复 | 列改动后执行 |
| 高恢复成本 | 可逆但找回代价高（改配置/参数/索引） | 列后果，等用户"可以" |
| 不可逆 | 删数据/改外部状态/发布/强推 | 标 `[不可逆]`，列后果，等用户拍板 |

**写入前清单**（每次写入/修改文件前 MUST 对照，缺一项视为未完成）：

1. **git 状态**：先 `git status -s`，检查 .env/密钥/构建产物。提交前逐项判断未跟踪文件，不默认 `git add .`
2. **权限判断**：按权限分级表确定行为
3. **文件大小**：目标文件行数 ≥ 400 或函数/方法数 ≥ 10 时，MUST 先声明该文件当前职责列表（一行一个），判断本次改动是否引入新职责。引入新职责 → 停下拍板是否先拆再写；原有职责 → 继续执行。discover 台账内文件每次修改均触发此检查
4. **错误信息硬约束**（写 raise/throw/抛业务异常或 catch/except 块时即时检查，不到闭包）：
   - 业务异常消息 MUST 含：字段名 + 期望值 + 实际值 + 业务上下文（如批次号/订单号/行号），缺一即视为本次写入未完成，不得继续下一步
   - catch/except 块 MUST 附业务上下文再继续抛或转换，不得裸 catch 后只 log 一行原始异常
   - 调试信息走 DEBUG 级别且受模块开关控制，不得用 print 或顶层 logger.info 打调试栈

**自愈**：失败后只有一次机械自愈权，仅限修命令参数/格式化/import/路径拼写，不得改目标/工具/范围/锚点字段。第二次失败 → 立即进异常态，下一个动作只能是输出异常态强制内容，不得执行任何其他操作。

**轮询**：操作结果依赖外部状态（页面渲染/接口响应/文件生成）时，MUST 优先用轮询等待而非判断一次就下结论。轮询三要素必须明确：① 检查信号（什么变化算成功）；② 间隔与上限（避免无限等待）；③ 超时后的处理（进异常态还是降级）。轮询不计入自愈次数。

**规则审计**：改规则/工具/hook/CI 时跑 `python3 tools/check.py rule-tool-contracts`。

**子 agent 复核**：中高风险或阶段闭包时派子 agent 按 `rules/engineering/code.index.md` 执行复核。子 agent 不得替用户拍板、改外部状态、执行不可逆动作。

**执行检查原则**：可逆/幂等操作优先乐观执行，执行后将实际结果与验收动作对照；不符合项才进专项检查循环修复，修复后再对照，直到通过。不可逆操作（删数据/外部写入/发布）仍需前置确认，不适用乐观模式。涉及拆分时按 `rules/engineering/code.index.md` § 拆分专项检查 跑（老调用 / 循环依赖 / 重复逻辑 / 接口完整性 / 测试覆盖 / 兼容别名 / MRO 冲突）。

**退出**：自愈失败 → 异常态；阶段产物完成 → 闭包态。


### 异常态

**进入**：测试失败 / 预期外行为 / 假设被推翻 / 自愈失败（具体规则见执行态自愈段）。
**自主度**：归零。禁止继续执行，禁止自行换方案。进入时 MUST 立即打断当前输出，通知用户进入异常态，不得静默等待。

**强制输出**（四项必填）：
- 现象：具体失败表现
- 可能原因：每条附对应业务表现，供用户对照排除
- 需用户做什么：明确的下一步请求
- 定位线索：日志路径 / 关键栈 / 最小复现命令 三选一即可，采不到 MUST 写「采不到的原因」，不许空过

**退出**（全满足）：根因查明 + 修复验证通过 + 测试通过 → 执行态。
用户说"问题解决了"时 MUST 将根因+解决方式写入项目 memory，type=feedback。

### 闭包态

**进入**：阶段产物完成。
**自主度**：只写闭包产物和检查点，不开始新执行。

**强制产物**：

| 字段 | 内容 |
|---|---|
| 已拍板 | 本阶段用户拍板的决策 |
| 假设终态 | 每条：已确认 / 已推翻 / 显式 defer |
| defer 项 | 明确推迟的内容（无则写"无"，禁止省略此字段）|
| 模块健康 | 本阶段写入/修改的文件中，行数 ≥ 400 或职责 ≥ 2 的文件列表；无则"无"；有则必须出现在 defer 项 |
| 错误信息质量 | 按执行态「写入时错误信息硬约束」清单抽查未命中条目；无新增则不出 |
| 下一阶段焦点 | 下一阶段要解决的核心问题，与谋划态「阶段重心」对接 |

**检查点快照**（仅在新开窗口恢复或用户明确要求时输出，强制产物已覆盖的字段不重复）：

| 字段 | 内容 |
|---|---|
| 任务级别 | T0/T1/T2 |
| 当前锚点 | 本阶段最近一次执行态锚点 |
| 活跃指令+T0文件 | 未完成指令及 T0 临时文件列表 |
| 测试状态 | 通过/失败/未运行，附时间 |

承重契约落盘到 git 项目 rule 文件。

**退出**：闭包产物完成 + 用户确认 → 谋划态（下一阶段）或归档。

## § 2 每轮输出格式

每轮 MUST 顶头一行，不再用表格。设计目标：信噪比最高，无内容字段整行不出。

**首行（每轮固定）**：
```
【X】 重心：Y | 在做：A / 因为 B
```

- `【X】`：状态单字（谋/执/异/闭），顶头
- `重心：Y`：阶段重心一句话，谋划态拍板时定，闭包前不变（回锚用）
- `在做：A / 因为 B`：A 段写具体动作（文件:函数 / 命令+参数 / 编辑对象），B 段写思路（为什么这么走）。逻辑常态化，每轮都出，不只是打断时

**追加段**（按需出，无内容整行不出，禁止写"无"占位）：
- 谋：`待拍板：` 编号列出
- 执：`未验证：` 仅本轮新增赌注（已列过不重复）
- 异：`现象：` / `可能原因：` / `需用户：` / `定位线索：`（四项必填，定位线索采不到 MUST 写「采不到的原因」）
- 闭：`defer：` / `活跃假设：` / `活跃指令+T0：`

**关注段**（凌驾追加段，AI 察觉到 MUST 出，无察觉不出）：

察觉判定 = 写代码时发现现有结构不优雅 / 跑测试时发现耗时不正常 / 用某方案时发现绕路 / 看到重复逻辑 / 看到可缓存但没缓存 / 任何「我注意到 X 但本轮不动」的念头。MUST 落到关注段，禁糊弄过去。

```
关注：
[问题] X 卡住 / 看着不对，因为 Y
[待优化] X 现在 Y 写法，未来可改 Z，本轮不做
```

**§ -1 打断当轮**：直接复用首行格式，B 段写当前思路即可。「不下判断、不辩护」是写法约束——B 段禁写「我觉得我没偏」「我应该是对的」，只摆事实给用户判断。打断当轮额外要求：A 段必须具体到文件+函数 / 命令+参数，禁抽象词（优化 / 重构 / 完善）。

**中文规则**：文档 / 对话回复 / 代码注释 / 异常消息 / commit message 全用中文，技术术语原文保留（如 `git`/`SSOT`）。

## § 3 测试规范

文件顶部 MUST 标注：
```
# 生命周期：T0 一次性（删除条件：XXX） / 持久维护
# 覆盖的业务阶段：
# 依赖的服务/环境：
# 运行方式：
```

按业务功能模块分组，不按技术层分组。

**新测试文件创建触发**（写新测试 MUST 按以下判定，默认行为是新建独立文件）：
- 新功能内部上下依赖的多个步骤（A 产物喂 B）→ 合并到**同一个新测试文件**，每步开头 `print("[步骤名] 开始")`，结束 `print("[步骤名] 通过/失败")`
- 与既有测试文件无业务依赖 → **不得**往老测试文件塞新功能（老测试代表已验证业务，混入新功能会让根因定位失效）
- 仅当新功能是修老功能的延伸且共享全部环境/夹具 → 才允许加入老文件，MUST 写「加入理由 + 依赖的老夹具列表」

**测试启动等待触发**（仅当测试需要切换窗口/进程时执行；否则禁止加任何启动 sleep）：
- 按执行态「轮询」规则执行，信号选一个：窗口标题匹配 / 进程名+PID / 端口监听 / 目标 DOM 元素出现
- 默认 0.5s 间隔、10s 超时（具体值可调）
- 禁止 `time.sleep(N)` 替代轮询；仅当业务时序本身要求等固定时长（如「等 5 秒后系统超时」是被测行为）才允许 sleep，且 MUST 注释业务原因
- 不切换窗口/进程的测试 MUST 显式写「无窗口/进程切换」，不加轮询

执行：
- 运行前检查环境（端口/服务存活），失败直接 `exit` 说明原因
- 测试间状态隔离，不依赖上一条副作用

输出：
- `✓ [业务描述]`
- `✗ [业务描述] | 期望: X | 实际: Y`
- `[ENV_ERROR] [描述]` — 环境问题
- `[LOGIC_ERROR] ✗ [业务描述]` — 业务逻辑问题

测试数据内联在文件顶部或函数内，附明确来源注释。

## § 4 环境默认

**新文件/新包创建通用约束**（语言无关，违反即禁止写第一行实现）：

新文件写入前 MUST 在文件顶部写入职责声明块，内容按 §1 槽⑥四项（唯一职责/不做什么/允许依赖层/谁不应该 import），格式按语言：

| 语言 | 格式 |
|---|---|
| Python | `# 职责：...` / `# 不做什么：...` / `# 允许依赖层：...` / `# 谁不应该 import：...` |
| TypeScript | 文件顶部 JSDoc `/** @module ... @not ... @deps ... @nocaller ... */` |
| Go | `doc.go` 或包注释 `// Package xxx ...；不做什么：...；允许依赖层：...；不应被 import：...` |

工具 `python3 tools/check_module_boundary.py` 可扫描 Python 文件的注释合规性（存量文件为 manual，新文件即时约束）。

- 可重建产物不迁移，只迁移重建声明：`.venv`/`node_modules`/编译产物/含本地路径的配置 MUST 加入 `.gitignore`；项目根 MUST 有换机器一步重建的入口脚本（`setup.bat`/`Makefile` 等），不得依赖硬编码用户路径
- 运行时产物不属于代码：`output/`/`logs/`/`cache/` 等运行时生成目录 MUST 加入 `.gitignore`；同步/备份脚本 MUST 显式排除（如 `rsync --exclude=output/`），不得依赖 `--delete` 的默认行为
- **语言专项约束**：按 `rules/engineering/polyglot.index.md` § 4
- 新增依赖或引入第三方调用时 MUST 判断是否有更合适替代；有则用 `[替代]` 标出，说明优势与代价
- 引入第二语言 = 不可逆跨语言契约边界，必须停下拍板；MUST 产出：边界在哪 / 谁负责校验序列化 / 类型漂移是否静默坏 / 工具链成本

## § 5 专题路由

| 触发 | 专题 |
|---|---|
| ML 指标/模型/数据管道 | `rules/process/modes.index.md` |
| 设计模式/抽象取舍 | `rules/engineering/code.index.md` |
| 多语言/TypeScript/Go | `rules/engineering/polyglot.index.md` |
| 工具契约 | `config/tooling.registry.toml` |
